# SQLite databases
*.db
*.db-journal
*.db-wal
*.db-shm

# Daemon runtime files
daemon.lock
daemon.log
daemon.pid
bd.sock

# Legacy database files
db.sqlite
bd.db

# Local version tracking (machine-specific)
.local_version
last-touched

# Keep JSONL exports and config (source of truth for git)
!*.jsonl
!metadata.json
