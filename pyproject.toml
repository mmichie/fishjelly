[project]
name = "fishjelly-tests"
version = "0.1.0"
description = "Test suite for Fishjelly HTTP server"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
    "websockets>=12.0",
    "h2>=4.1.0",
]

[dependency-groups]
dev = [
    "pytest>=7.4.0",
    "pytest-asyncio>=0.21.0",
]
